/**
 * A custom state in which a specified cutscene plays.
 * 
 * By leaving the previous scene before playing the video,
 * we prevent a couple bugs caused by interacting with the previous scene.
 */
class CustomCutsceneState {}
